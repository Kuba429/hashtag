import React from "react";

export default function LoggedStatus() {
    return (
        <li className="">
            {/* a tag for now in order to keep consistent styling */}
            <a>Login / Register</a>
        </li>

      
    );
}
